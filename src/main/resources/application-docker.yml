
spring:
  application:
    name: chat-api
  config:
    activate:
      on-profile: "docker"

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka:8761/eureka